# ملخص Foundations (F.1 - F.5) — System Design for Vibe Coders

---

## F.1 — إيه اللي بيحصل لما تفتح موقع (The Four-Step Trip)

**الفكرة:**
لما تفتح أي موقع، بيحصل 4 خطوات بالترتيب:

1. جهازك يسأل الـ **DNS** (زي دليل التليفون) عن العنوان الحقيقي للموقع.
2. الـ DNS بيرجعله بالعنوان (address).
3. جهازك يبعت **request** ("يطرق الباب").
4. الـ **server** يرد بـ **response** — الصفحة نفسها.

فيه حاجة اسمها **CDN**: نسخة قريبة من الموقع، بتخلي الرد يوصلك أسرع من غير ما تروح للـ server الأصلي (origin) كل مرة.

**درس مهم:** أي فكرة موقع (حتى لو معقدة) بتمر بنفس الأربع خطوات دي بالظبط — مفيش حاجة "مختلفة" في الأساس.

ISP = يوصلني بالإنترنت

DNS = يعرفني أروح فين

CDN = يجيبلي المحتوى من أقرب مكان ممكن

**اللي عملناه عمليًا:**

- عملنا رسم (diagram) لرحلة طلب صفحة Wikipedia.
- عملنا رسم لموقع "camera rental" وشفنا إن الفرق الوحيد كان إضافة **Database** في الآخر.
- قسنا فعليًا الوقت (round-trip time) لـ 3 مواقع مختلفة باستخدام `curl -w`.

---

## F.2 — إيه هو الـ Server فعلاً (وإيه هو الـ Code)

**الفكرة:**

- **Server** = مجرد **computer** عادي، وظيفته إنه يفضل شغال ومتصل بالإنترنت طول الوقت.
- **Code** = زي الـ **recipe** (وصفة) — تعليمات مكتوبة بس.
- **Running program** = الطبخ وهو شغال فعليًا.
- **Database** = الـ **notebook** — أي حاجة لازم متتنساش (accounts, orders...).

**نقطة مهمة زودناها إحنا:**
فيه فرق بين:

- **الكود نفسه** (الملفات) — موجود دايمًا على جهازك.
- **الـ process** (البرنامج شغال فعليًا في الذاكرة) — بيموت لما تقفل الجهاز أو توقف البرنامج.

يعني: "الكود موجود" ≠ "الموقع شغال (live)".

**سؤالك المهم:** أي جهاز (حتى لابتوبك) ممكن يبقى server، شرط إنه يفضل شغال ومتصل بالنت 24 ساعة — بس عمليًا مش عملي، عشان كده بنستأجر أجهزة في **data centers** مبنية أصلًا عشان متقعش.

**localhost:** لما موقعك شغال على localhost، معناه شغال جوه جهازك بس — محدش تاني في الدنيا يقدر يوصله، لأنه مش عنده عنوان عام على الإنترنت.

---

## F.3 — إزاي السوفتوير بيتحرك (Versions, Repos, Deploys)

**الفكرة:**

- **Repo** = تاريخ كامل لكل نسخة (draft) اتحفظت من مشروعك، بأداة اسمها **git**.
- **Commit** = حفظ نسخة جديدة مع ملاحظة بتشرح إيه اللي اتغير.
- **Deploy** = تاخد نسخة معينة من الـ repo وتحطها شغالة على الـ server الحقيقي (الـ always-on).
- **Rollback** = ترجع لنسخة قديمة، زي اختيار من قايمة.

**الـ Environments (3 أماكن للموقع نفسه):**

| Environment | فين شغال                   | مين بيشوفه         |
| ----------- | -------------------------- | ------------------ |
| Local       | جهازك (localhost)          | إنت بس             |
| Staging     | server تجريبي، شبه الحقيقي | إنت وفريقك للتجربة |
| Production  | الـ server الحقيقي         | كل الناس           |

**قصة تحذيرية:** Knight Capital خسرت 440 مليون دولار في 45 دقيقة لأن الـ deploy وصل لـ 7 servers من 8 — سيرفر واحد فضل شغال بنسخة قديمة.

**الكوماندز الأساسية اللي اتعلمناها:**

```bash
git init            # يبدأ الـ repo
git add .           # يجهز الملفات للحفظ
git commit -m "..."  # يحفظ نسخة جديدة
git log             # يوري كل النسخ
git diff v1 v2      # يوري الفرق بين نسختين
git checkout v1     # يرجعك لنسخة قديمة (rollback)
git push origin main # يبعت الكود لمكان مركزي زي GitHub
```

---

## F.4 — قابل الـ Agent بتاعك (Directing a Builder You Can't Watch)

**الفكرة:**

1. الـ agent شايف "شباك" بس، مش العالم كله — كل session بيبدأ من الصفر. الحل: ملف **CLAUDE.md** فيه إيه المنتج، إيه الممنوع، وإزاي تحب يديك دليل.
2. أي تعليمة كويسة فيها 3 أجزاء: **Goal** (هدف واضح) + **Constraints** (حدود) + **Evidence** (إثبات إنه خلص فعلاً).
3. أي تصحيح تكرره مرتين → لازم يتحول لـ rule دايمة في CLAUDE.md.
4. أي حاجة خطيرة (مسح/حذف/فلوس) لازم الـ agent يسأل "موافق؟" الأول.

**اللي عملناه عمليًا:**
عملنا أول ملف **CLAUDE.md** لمشروعك (موقع متابعة الأهداف الستة)، وحطينا فيه:

- المشروع لاستخدامك الشخصي بس (من غير حسابات متعددة).
- ممنوع مسح أو تعديل الـ database من غير ما يسألك، لأن بيانات الأهداف هي الأهم.

---

## F.5 — أول صفحة Live بإثبات (Evidence over Trust)

**الفكرة الأساسية (روح الكورس كله):**

> "Done" هو **ادعاء (claim)**. الـ **Evidence** هو **حقيقة**. متقبلش الادعاء لما الدليل بعده طلب واحد بس.

**قواعد الـ Evidence الحقيقي:**

- يجيلك من **مكان المستخدم** (موبايلك، مش صورة شاشة من الـ agent).
- يعيش **نظرة جديدة** (جهاز/شبكة مختلفة) — عشان الـ caching ممكن يورّيك نسخة قديمة.
- تعرف **طريقة الرجوع** قبل ما تحتاجها.

**اللي عملناه عمليًا (بالترتيب):**

1. عملنا صفحة عن **الصحة النفسية** (title + 3 جمل + تاريخ)، وعملنا commit أول (v1).
2. عملنا push للكود على GitHub.
3. أول محاولة deploy كانت على رابط خاص (session-specific) — اشتغل على اللابتوب بس مش على الموبايل. اكتشفنا إن ده مش رابط عام حقيقي.
4. عملنا deploy تاني على **GitHub Pages** (`username.github.io/repo-name`) — رابط عام حقيقي.
5. اتأكدنا من الموبايل على شبكة مختلفة — شغال فعلًا (evidence حقيقي، مش claim).
6. عملنا تغيير بسيط (v2) وشفناه ظهر بسرعة من غير تأخير caching.
7. طلبنا من الـ agent يكتب **rollback runbook** (خطوات الرجوع لـ v1) في ملف README — من غير ما ننفذه فعليًا، بس عشان الخطة تكون جاهزة قبل ما نحتاجها.

**درس إضافي اتعلمناه بالتجربة:**

- لازم تفرق بين الـ **private source repo** (اللي بتشتغل عليه) والـ **public deployment repo** (اللي منشور فعليًا) — لو عندك اتنين، أي تغيير أو rollback لازم يتعمل في الاتنين.
- `git revert` (بيضيف commit جديد بيلغي تأثير القديم، آمن) مختلف تمامًا عن `git reset` (بيمسح التاريخ، خطير) — استخدم `revert` دايمًا للـ rollback.

---

## الخلاصة: الخمس أفكار اللي كل حاجة جاية بتتبني عليها

1. **الرحلة (The Trip):** أي موقع بيمر بنفس 4 خطوات (DNS → address → request → response).
2. **المطبخ (The Kitchen):** server = computer عادي شغال طول الوقت؛ code = وصفة؛ process = طبخ فعلي.
3. **المسودات (The Drafts):** git بيحفظلك تاريخ كامل، والـ deploy هو نقل نسخة معينة للـ server الحقيقي.
4. **عقد المدير (The Director's Contract):** وجّه الـ agent بـ Goal + Constraints + Evidence، ومتنساش الـ CLAUDE.md.
5. **الدليل فوق الثقة (Evidence over Trust):** "Done" كلام؛ شوف بعينك من مكان المستخدم الحقيقي.
